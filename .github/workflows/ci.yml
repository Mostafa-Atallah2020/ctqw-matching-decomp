name: CI

on: [push, pull_request]

jobs:
  setup:
    runs-on: windows-latest
    strategy:
      matrix:
        python-version: [3.9, 3.10, 3.11]

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install isort black flake8 mypy pylint pytest pytest-cov
        pip install -r requirements.txt

    - name: Set PYTHONPATH
      run: |
        echo "PYTHONPATH=$(pwd)/src" >> $GITHUB_ENV

  # lint:
  #   runs-on: windows-latest
  #   needs: setup
  #   strategy:
  #     matrix:
  #       tool: [isort, black, flake8, mypy, pylint]
  #   steps:
  #   - name: Checkout Repository
  #     uses: actions/checkout@v2

  #   - name: Set up Python ${{ matrix.python-version }}
  #     uses: actions/setup-python@v2
  #     with:
  #       python-version: ${{ matrix.python-version }}

  #   - name: Install dependencies
  #     run: |
  #       python -m pip install --upgrade pip
  #       pip install ${{ matrix.tool }}
  #       pip install -r requirements.txt

  #   - name: Run ${{ matrix.tool }}
  #     run: |
  #       if [ "${{ matrix.tool }}" = "isort" ]; then
  #         isort . --check-only
  #       elif [ "${{ matrix.tool }}" = "black" ]; then
  #         black . --check
  #       elif [ "${{ matrix.tool }}" = "flake8" ]; then
  #         flake8
  #       elif [ "${{ matrix.tool }}" = "mypy" ]; then
  #         mypy .
  #       elif [ "${{ matrix.tool }}" = "pylint" ]; then
  #         pylint $(find . -name "*.py")
  #       fi

  pytest:
    runs-on: windows-latest
    needs: setup
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e .[lint,test]
        pip install pytest pytest-cov
        pip install -r requirements.txt

    - name: Set PYTHONPATH
      run: |
        echo "PYTHONPATH=$(pwd)/src" >> $GITHUB_ENV

    - name: Run PyTest
      run: pytest -v #--cov
